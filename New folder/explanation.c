#include <stdio.h>
int main()
{
    int a = 20;
    int b = 20;
    if (a != b)
    {
        printf("a and b are equal\n");
    }
    else
    {
        printf("a and b are not equal\n");
    }

    // != is the not equal operator in C.It is used to compare two values and returns true if they are not equal, and false if they are equal.For example :

    return 0;
}